import LoginForm from "./LoginForm";

export const metadata = {
  title: "Login - Tap2Order",
  description: "Login to your Tap2Order account to manage your tasks and orders efficiently.",
  keywords: ["login", "tap2order", "nextjs", "authentication"],
};

export default function Page() {
  return <LoginForm />;
}
